var valores = [0, 3, 6, 4, 2, 5, 1]
    for(var pos=0; pos < valores.length; pos++){
        console.log(`a posição ${pos} tem o valor ${valores[pos]}`)
    }

    /*
    for( var pos in valores){
        console.log(`a posição${pos} tem o valor ${valores[num]}`)
    }
    */